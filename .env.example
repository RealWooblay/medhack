# Browser-safe build settings. VITE_ values are public after the app is built.
VITE_MEDGEMMA_ENDPOINT=/api/clinical-review

# Vercel-to-Google Cloud identity federation. Keep every value server-side.
GCP_PROJECT_ID=
GCP_PROJECT_NUMBER=
GCP_SERVICE_ACCOUNT_EMAIL=
GCP_WORKLOAD_IDENTITY_POOL_ID=
GCP_WORKLOAD_IDENTITY_POOL_PROVIDER_ID=
# HTTPS custom audience used to mint the Vercel OIDC token; see DEPLOYMENT.md.
GCP_AUDIENCE=

# Real MedGemma deployment created from Vertex AI Model Garden.
MEDGEMMA_VERTEX_LOCATION=
MEDGEMMA_VERTEX_ENDPOINT_ID=
# Exact serving model ID sent to the pinned MedGemma endpoint.
MEDGEMMA_MODEL_ID=google/medgemma-27b-text-it
# Optional hostname only, returned as dedicatedEndpointDns by Vertex AI.
MEDGEMMA_VERTEX_DEDICATED_DNS=

# Real PharmCAT service. The browser must upload raw genome files directly to
# the private bucket using a run-scoped upload session created by the API.
PHARMCAT_SERVICE_URL=
PHARMCAT_SERVICE_AUDIENCE=
# Generate at least 32 random bytes; this signs the private browser-session cookie.
PHARMCAT_SESSION_SECRET=

# Private Cloud Run control service settings (not Vercel browser variables).
PHARMCAT_RUN_BUCKET=
PHARMCAT_JOB_NAME=projects/PROJECT_ID/locations/australia-southeast1/jobs/pharmcat-worker
PHARMCAT_JOB_CONTAINER=worker
PHARMCAT_IMAGE=pgkb/pharmcat:3.3.0@sha256:e9b02865a0abe1a0085ac0d7625f1ec33a06a56e9571d5befb16f90d4fedc435
# Digest of the custom worker image deployed to the Cloud Run Job.
PHARMCAT_WORKER_IMAGE_DIGEST=sha256:REPLACE_WITH_64_HEX_CHARACTERS
PGX_APP_ORIGIN=https://REPLACE_WITH_VERCEL_ORIGIN
